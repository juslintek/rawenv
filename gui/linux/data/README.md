# Data Directory

The application loads mock data from `../../shared/mock-data.json` (relative to the build directory).

To create a symlink for development:
```bash
ln -sf ../../shared/mock-data.json gui/linux/data/mock-data.json
```
