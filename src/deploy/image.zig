const std = @import("std");
const config = @import("config");

pub fn generateContainerfile(allocator: std.mem.Allocator, cfg: config.Config) ![]const u8 {
    var buf: std.ArrayList(u8) = .empty;
    defer buf.deinit(allocator);
    const w = buf.writer(allocator);

    try w.print("# Generated by rawenv — {s}\n", .{cfg.project_name});
    try w.writeAll(
        \\FROM debian:13-slim
        \\
        \\RUN apt-get update && apt-get install -y curl && \
        \\    curl -fsSL https://rawenv.sh/install | sh && \
        \\    apt-get clean && rm -rf /var/lib/apt/lists/*
        \\
        \\WORKDIR /app
        \\COPY rawenv.toml .
        \\RUN rawenv init
        \\
    );

    for (cfg.services) |svc| {
        if (std.mem.eql(u8, svc.key, "postgresql") or std.mem.eql(u8, svc.key, "postgres")) {
            try w.writeAll("EXPOSE 5432\n");
        } else if (std.mem.eql(u8, svc.key, "redis")) {
            try w.writeAll("EXPOSE 6379\n");
        } else if (std.mem.eql(u8, svc.key, "mysql")) {
            try w.writeAll("EXPOSE 3306\n");
        }
    }

    try w.writeAll("\nCMD [\"rawenv\", \"up\"]\n");

    return try buf.toOwnedSlice(allocator);
}

pub fn generateVMCloudInit(allocator: std.mem.Allocator, cfg: config.Config) ![]const u8 {
    var buf: std.ArrayList(u8) = .empty;
    defer buf.deinit(allocator);
    const w = buf.writer(allocator);

    try w.writeAll(
        \\#cloud-config
        \\# Generated by rawenv
        \\
        \\package_update: true
        \\packages:
        \\  - curl
        \\
        \\runcmd:
        \\  - curl -fsSL https://rawenv.sh/install | sh
        \\  - mkdir -p /opt/rawenv
        \\  - rawenv init
        \\  - rawenv up
        \\
        \\write_files:
        \\  - path: /opt/rawenv/rawenv.toml
        \\    content: |
        \\
    );

    try w.print("      [project]\n      name = \"{s}\"\n", .{cfg.project_name});

    if (cfg.services.len > 0) {
        try w.writeAll("      [services]\n");
        for (cfg.services) |svc| {
            try w.print("      {s} = \"{s}\"\n", .{ svc.key, svc.value });
        }
    }

    return try buf.toOwnedSlice(allocator);
}
